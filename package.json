{
  "name": "cryptobot-live",
  "version": "1.0.0",
  "description": "CryptoBot LIVE — Capital real, adopta params del PAPER con umbral",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "TICK_MS=5000 node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
